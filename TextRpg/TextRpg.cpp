
#include <iostream>
#include <list>
#include "PrintInfo.h"
#include "City.h"

using namespace std;



int main()
{
            
    //list<tileInfo> 구현하기 내가 볼땨는 tile.h 에 구현하면 좋을 듯
    //list에 값을 담아서 값을 뽑아낸 후 그 정보를 바탕으로 vector find 해서 각각의 정보를 찾으면 좋을 듯

    
    City city = City("name","type",0);
    
    /*PrintInfo *activityManager = new PrintInfo();
    activityManager->InitActivity();
    activityManager->IntroActivity();
    
    int temp = NULL;
    cin >> temp;
    
    vector<Player> player;
    vector<City> citys;

    Player *cus = new Player();
    player.push_back(*cus);



    while(NULL !=temp)
    {
        activityManager->LandActivity(player,citys);
        cin >> temp;
    }*/
    
}

